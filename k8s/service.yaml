apiVersion: v1
kind: Service
metadata:
  name: ai-devops-optimizer
  labels:
    app: ai-devops-optimizer
spec:
  type: ClusterIP
  selector:
    app: ai-devops-optimizer
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
